import SectionHeader from "./section-header";

export default function HabitsSection() {
    return (
        <section className="flex flex-col gap-6">
            <SectionHeader title="Habits" id="habits" />
        </section>
    );
}